# Docker Оптимизация для Production

## 📊 Анализ текущего Dockerfile

### ✅ Что уже хорошо реализовано:

- **Многостадийная сборка** - разделение на deps, builder, runner
- **Non-root пользователь** - безопасность
- **Health check** - мониторинг состояния
- **Prisma интеграция** - правильная генерация клиента
- **Standalone режим** - оптимизация для Docker

### ❌ Проблемы и области для улучшения:

#### 1. **Дублирование кода**

```dockerfile
# Проблема: дублирование yarn.lock
COPY yarn.lock* ./
COPY yarn.lock* ./
```

#### 2. **Отсутствие .dockerignore**

- Копируются ненужные файлы (тесты, документация, IDE файлы)
- Увеличивается размер контекста сборки

#### 3. **Неоптимальная установка зависимостей**

- Отсутствуют флаги оптимизации
- Нет очистки кэша

#### 4. **Проблемы с сигналами**

- Отсутствует dumb-init для правильной обработки SIGTERM
- Могут быть проблемы с graceful shutdown

#### 5. **Отсутствие production зависимостей**

- В финальном образе нет node_modules
- Может привести к ошибкам runtime

## 🚀 Рекомендации по улучшению

### 1. **Создание .dockerignore**

```dockerignore
# Зависимости
node_modules
npm-debug.log*

# Тестирование
coverage
test-results
playwright-report

# Разработка
.env.local
.vscode
.idea

# Документация
docs
*.md
!README.md

# Docker
Dockerfile*
docker-compose*.yml

# CI/CD
.github

# Логи
*.log
```

### 2. **Оптимизация установки зависимостей**

```dockerfile
# Добавить флаги оптимизации
RUN npm ci --only=production --no-audit --no-fund --prefer-offline && \
    npm cache clean --force
```

### 3. **Добавление dumb-init**

```dockerfile
# Установка dumb-init
RUN apk add --no-cache dumb-init

# Использование в ENTRYPOINT
ENTRYPOINT ["dumb-init", "--"]
CMD ["node", "server.js"]
```

### 4. **Копирование production зависимостей**

```dockerfile
# Копируем только production зависимости
COPY --from=deps /app/node_modules ./node_modules
```

### 5. **Оптимизация переменных окружения**

```dockerfile
# Группировка ENV команд
ENV NODE_ENV=production \
    NEXT_TELEMETRY_DISABLED=1 \
    PORT=3000
```

## 📈 Ожидаемые улучшения

### Размер образа:

- **До оптимизации**: ~800MB
- **После оптимизации**: ~400-500MB
- **Экономия**: ~40-50%

### Время сборки:

- **До оптимизации**: 5-8 минут
- **После оптимизации**: 3-5 минут
- **Ускорение**: ~30-40%

### Безопасность:

- ✅ Правильная обработка сигналов
- ✅ Non-root пользователь
- ✅ Минимальный набор зависимостей
- ✅ Отсутствие dev-зависимостей в production

## 🔧 Дополнительные рекомендации

### 1. **Multi-stage оптимизация**

```dockerfile
# Отдельная стадия для Prisma
FROM base AS prisma
COPY prisma ./prisma
RUN npx prisma generate
```

### 2. **Кэширование слоев**

```dockerfile
# Копируем package.json отдельно для лучшего кэширования
COPY package*.json ./
RUN npm ci --only=production
COPY . .
```

### 3. **Оптимизация для CI/CD**

```dockerfile
# Использование BuildKit для параллельной сборки
# syntax=docker/dockerfile:1
```

### 4. **Мониторинг и логирование**

```dockerfile
# Добавление метрик
LABEL maintainer="your-team@company.com"
LABEL version="1.0.0"
LABEL description="P App"
```

## 🧪 Тестирование оптимизаций

### Команды для тестирования:

```bash
# Сборка образа
docker build -t parket-crm:optimized -f docker/production/node/Dockerfile.optimized .

# Проверка размера
docker images parket-crm

# Тестирование запуска
docker run -p 3000:3000 parket-crm:optimized

# Проверка health check
curl http://localhost:3000/api/health
```

### Метрики для отслеживания:

- Размер образа
- Время сборки
- Время запуска
- Использование памяти
- Время отклика health check

## 📋 Чек-лист оптимизации

- [ ] Создан .dockerignore
- [ ] Убрано дублирование кода
- [ ] Добавлены флаги оптимизации npm
- [ ] Установлен dumb-init
- [ ] Копируются production зависимости
- [ ] Оптимизированы переменные окружения
- [ ] Настроен правильный ENTRYPOINT
- [ ] Протестирован размер образа
- [ ] Протестировано время сборки
- [ ] Протестирован запуск приложения
- [ ] Протестирован health check
- [ ] Протестирован graceful shutdown

## 🎯 Следующие шаги

1. **Внедрить оптимизации** в основной Dockerfile
2. **Протестировать** в dev окружении
3. **Обновить CI/CD** пайплайны
4. **Мониторить** метрики в production
5. **Документировать** изменения для команды
