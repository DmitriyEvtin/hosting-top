# Быстрый старт с MailHog

## 🚀 Запуск за 3 шага

### 1. Запустите MailHog

```bash
# Запуск MailHog
make mailhog-up

# Или через docker compose
docker compose up -d mailer
```

### 2. Настройте переменные окружения

Создайте файл `.env.local`:

```bash
# Email настройки для MailHog
SMTP_HOST="localhost"
SMTP_PORT="1025"
SMTP_USER=""
SMTP_PASSWORD=""
SMTP_FROM="noreply@rolled-metal.local"
```

### 3. Запустите приложение

```bash
npm run dev
```

## 🧪 Тестирование

### Автоматическое тестирование

```bash
# Запуск тестов email
npm run test:email

# Проверка статуса MailHog
make mailhog-status

# Очистка писем
make mailhog-clear
```

### Ручное тестирование

1. Откройте http://localhost:3000/admin/email
2. Введите тестовый email
3. Отправьте письмо
4. Проверьте в MailHog UI: http://localhost:8025

## 📱 Интерфейсы

- **Приложение:** http://localhost:3000
- **MailHog UI:** http://localhost:8025
- **Adminer:** http://localhost:8080

## 🔧 Полезные команды

```bash
# Управление MailHog
make mailhog-up      # Запуск
make mailhog-down    # Остановка
make mailhog-logs    # Логи
make mailhog-clear   # Очистка писем

# Тестирование
make test-email           # Отправка тестового письма
make test-email-status    # Проверка статуса
npm run test:email        # Полное тестирование
```

## 🐛 Troubleshooting

### MailHog не запускается

```bash
# Проверьте статус
docker ps | grep mailhog

# Посмотрите логи
make mailhog-logs

# Перезапустите
make mailhog-down && make mailhog-up
```

### Письма не доходят

1. Проверьте переменные окружения
2. Убедитесь, что MailHog запущен
3. Проверьте логи приложения

### MailHog UI недоступен

```bash
# Проверьте порт
curl http://localhost:8025

# Проверьте контейнер
docker ps | grep 8025
```

## 📋 Чек-лист

- [ ] MailHog запущен (`docker ps | grep mailhog`)
- [ ] Переменные окружения настроены
- [ ] Приложение запущено (`npm run dev`)
- [ ] MailHog UI доступен (http://localhost:8025)
- [ ] Тест email работает (`npm run test:email`)

## 🎯 Следующие шаги

1. **Настройте реальный SMTP** для production
2. **Добавьте email шаблоны** для ваших нужд
3. **Настройте мониторинг** email отправки
4. **Добавьте очереди** для массовой отправки

## 📚 Дополнительная документация

- [Полная настройка MailHog](./mailhog-setup.md)
- [Настройка Email сервиса](./email-setup.md)
- [Архитектура проекта](../architecture.md)
