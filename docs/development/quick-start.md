# Быстрый старт разработки

## 🚀 Настройка среды разработки за 1 минуту

```bash
# Единственная команда для запуска всего!
make dev
```

Эта команда автоматически:

- Остановит и удалит существующие контейнеры
- Удалит старые volumes (база данных будет пересоздана)
- Запустит все контейнеры (PostgreSQL, Redis, MinIO, MailHog, Adminer)
- Настроит базу данных (миграции + тестовые данные)
- Запустит приложение

## 🔑 Учетные данные

После выполнения `make dev` у вас будут доступны:

- **Администратор**: `admin@dev.ru` / `111111`
- **Пользователи**: `user@dev.ru`, `moderator@dev.ru`, `test@dev.ru` / `111111`

## 🌐 Сервисы

- **Приложение**: http://localhost:3000
- **PostgreSQL**: localhost:5432
- **Redis**: localhost:6379
- **Adminer**: http://localhost:8080 (автоматическое подключение к PostgreSQL)
- **MailHog**: http://localhost:8025
- **MinIO**: http://localhost:9001

## 📋 Полезные команды

```bash
# Полный сброс среды разработки
make dev-reset

# Остановка всех сервисов
make dev-down

# Просмотр логов
docker-compose logs -f
```

## ⚠️ Важные особенности

- **База данных НЕ сохраняется** между перезапусками
- При каждом `make dev` база создается заново
- Все пользователи имеют пароль `111111`
- Тестовые данные создаются автоматически

## 🛠️ Альтернативные команды

```bash
# Основные команды
make dev          # Настройка среды + запуск приложения
make dev-down     # Остановка
make dev-reset    # Полный сброс

# Дополнительные команды
make dev-setup    # Только настройка среды
make dev-clean    # Только очистка
```

## 🔧 Устранение проблем

### База данных не подключается

```bash
# Перезапустите PostgreSQL
docker-compose restart postgres

# Или полностью пересоздайте среду
make dev-reset
```

### Ошибки миграций

```bash
# Сбросьте базу данных
npm run db:reset

# Или пересоздайте среду
make dev-reset
```

### Проблемы с Docker

```bash
# Очистите Docker
docker system prune -f
docker volume prune -f

# Пересоздайте среду
make dev-reset
```

## 📚 Дополнительная документация

- [Полная документация среды разработки](./dev-environment.md)
- [Настройка проекта](./setup.md)
- [Архитектура проекта](../architecture.md)
