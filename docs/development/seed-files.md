# Seed файлы для инициализации базы данных

## Обзор

Проект использует два отдельных seed файла для разных целей:

1. **`prisma/seed.ts`** - инициализация (только администратор)
2. **`prisma/seed-test.ts`** - тестовые данные (пользователи, категории, товары)

## Доступные команды

### Инициализация (только администратор)

```bash
npm run db:seed
```

**Что создается:**

- ✅ Администратор с настраиваемыми учетными данными
- ✅ Безопасный пароль (случайный или из переменных окружения)

### Тестовые данные

```bash
npm run db:seed-test
```

**Что создается:**

- ✅ Тестовый пользователь

### Полная инициализация (администратор + тестовые данные)

```bash
npm run db:seed-all
```

**Что создается:**

- ✅ Все из инициализации
- ✅ Все из тестовых данных

## Настройка администратора

### Переменные окружения

Добавьте в `.env` файл:

```bash
# Администратор по умолчанию
ADMIN_EMAIL="admin@your-domain.com"
ADMIN_PASSWORD="your-secure-password"
ADMIN_NAME="Администратор"
```

### Без переменных окружения

Если переменные не указаны, система создаст:

- **Email**: `admin@parket-crm.ru`
- **Пароль**: Случайный (32 символа)
- **Имя**: `Администратор`

## Сценарии использования

### 🚀 Production развертывание

```bash
# Только администратор
npm run db:seed
```

### 🧪 Development с тестовыми данными

```bash
# Администратор + тестовые данные
npm run db:seed-all
```

### 🔄 Только тестовые данные (если админ уже есть)

```bash
# Только тестовые данные
npm run db:seed-test
```

### 🧹 Очистка и полная переинициализация

```bash
# Очистка базы данных
npm run db:reset

# Полная инициализация
npm run db:seed-all
```

## Безопасность

### Development

- Учетные данные выводятся в консоль
- Пароль: `111111` (для удобства разработки)

### Production

- Учетные данные НЕ выводятся в консоль
- Пароль: случайный (32 символа) или из переменных окружения
- **ОБЯЗАТЕЛЬНО** смените пароль после первого входа

## Troubleshooting

### Проблема: Администратор не создается

```bash
# Проверьте подключение к БД
npm run db:migrate

# Перегенерируйте Prisma клиент
npm run db:generate

# Запустите инициализацию
npm run db:seed
```

### Проблема: Ошибки при создании тестовых данных

```bash
# Убедитесь, что администратор существует
npm run db:seed

# Затем создайте тестовые данные
npm run db:seed-test
```

### Проблема: Дублирование данных

```bash
# Очистите базу данных
npm run db:reset

# Запустите полную инициализацию
npm run db:seed-all
```

## Лучшие практики

1. **Production**: Используйте только `npm run db:seed`
2. **Development**: Используйте `npm run db:seed-all`
3. **Тестирование**: Используйте `npm run db:seed-test`
4. **Всегда** настраивайте переменные окружения для production
5. **Обязательно** смените пароль администратора после первого входа
