# MinIO Quick Start Guide

## Быстрый старт с MinIO для локального тестирования

### 🚀 За 5 минут

```bash
# 1. Запуск MinIO
make minio-up

# 2. Настройка окружения
cp env.minio.example .env.local

# 3. Инициализация bucket
make minio-setup

# 4. Запуск приложения
make dev
```

### 📋 Что происходит

1. **MinIO запускается** на портах 9000 (API) и 9001 (Console)
2. **Создается bucket** `parket-crm-images` с CORS политикой
3. **Настраивается структура папок** для организации файлов
4. **Приложение подключается** к MinIO вместо AWS S3

### 🌐 Доступ к сервисам

- **Приложение:** http://localhost:3000
- **MinIO Console:** http://localhost:9001 (minioadmin / minioadmin123)
- **MinIO API:** http://localhost:9000

### 🧪 Тестирование загрузки

```bash
# Проверка статуса MinIO
make minio-status

# Тестирование API загрузки
curl -X POST http://localhost:3000/api/upload/image \
  -F "file=@test-image.jpg" \
  -F "category=test"
```

### 📁 Структура bucket

```
parket-crm-images/
├── images/
│   ├── products/     # Изображения товаров
│   ├── categories/   # Изображения категорий
│   └── thumbnails/   # Миниатюры
└── files/            # Другие файлы
```

### 🔧 Полезные команды

```bash
# Управление MinIO
make minio-up        # Запуск
make minio-down      # Остановка
make minio-restart   # Перезапуск
make minio-logs      # Логи
make minio-status    # Статус
make minio-console   # Открыть Console

# Настройка
make minio-setup     # Инициализация bucket
```

### 🐛 Troubleshooting

**MinIO не запускается:**

```bash
make minio-logs
docker-compose restart minio
```

**Bucket не создается:**

```bash
make minio-setup
```

**CORS ошибки:**

- Проверьте CORS в MinIO Console
- Очистите кэш браузера

### 📚 Дополнительно

- **Полная документация:** [minio-setup.md](./minio-setup.md)
- **AWS интеграция:** [aws-integration.md](./aws-integration.md)
- **Быстрый старт AWS:** [aws-quickstart.md](./aws-quickstart.md)

### 🎯 Следующие шаги

1. **Протестируйте загрузку** изображений через UI
2. **Проверьте создание миниатюр** в папке thumbnails
3. **Настройте production** с реальным AWS S3
4. **Изучите мониторинг** и логирование

---

**Время настройки:** ~5 минут  
**Сложность:** Низкая  
**Требования:** Docker, Node.js
