# Настройка администратора

Документация по созданию и управлению первым администратором системы.

## Обзор

Система использует гибридный подход для создания первого администратора:

- **Безопасность**: Случайный пароль по умолчанию
- **Гибкость**: Настройка через переменные окружения
- **Удобство**: Автоматическое создание при развертывании
- **Контроль**: Принудительная смена пароля через веб-интерфейс

## Создание администратора

### Автоматическое создание

При запуске `prisma db seed` администратор создается автоматически:

```bash
npm run db:seed
```

### Настройка через переменные окружения

Добавьте в файл `.env`:

```bash
# Администратор по умолчанию
ADMIN_EMAIL="admin@parket-crm.ru"
ADMIN_PASSWORD="your-secure-password-here"
ADMIN_NAME="Администратор"
```

### Переменные окружения

| Переменная       | Описание              | По умолчанию           |
| ---------------- | --------------------- | ---------------------- |
| `ADMIN_EMAIL`    | Email администратора  | `admin@parket-crm.ru`  |
| `ADMIN_PASSWORD` | Пароль администратора | Случайный (32 символа) |
| `ADMIN_NAME`     | Имя администратора    | `Администратор`        |

## Безопасность

### Генерация пароля

Если `ADMIN_PASSWORD` не указан, система генерирует случайный пароль:

```typescript
function generateSecurePassword(): string {
  return crypto.randomBytes(16).toString("hex");
}
```

### Хеширование пароля

Все пароли хешируются с помощью bcrypt:

```typescript
const hashedPassword = await bcrypt.hash(password, 12);
```

### Логирование

- **Development**: Учетные данные выводятся в консоль
- **Production**: Только подтверждение создания

## Управление паролем

### Смена пароля через веб-интерфейс

1. Войдите в систему
2. Перейдите на `/profile` (через выпадающее меню пользователя)
3. Нажмите "Изменить пароль"
4. Введите текущий и новый пароль

### API для смены пароля

```typescript
POST /api/user/change-password
{
  "currentPassword": "текущий_пароль",
  "newPassword": "новый_пароль"
}
```

### Требования к паролю

- Минимум 8 символов
- Рекомендуется использовать сложные пароли
- Пароль должен отличаться от текущего

## Профиль пользователя

### Доступ к профилю

URL: `/profile`

### Функции профиля

- Просмотр информации о пользователе
- Смена пароля
- Просмотр системной информации
- Отображение роли пользователя

### Навигация к профилю

Профиль доступен через выпадающее меню пользователя в шапке сайта.

## Развертывание

### Development

1. Скопируйте `.env.example` в `.env`
2. Настройте переменные администратора
3. Запустите миграции и seed:
   ```bash
   npm run db:migrate
   npm run db:seed
   ```

### Production

1. Установите переменные окружения:

   ```bash
   export ADMIN_EMAIL="admin@your-domain.com"
   export ADMIN_PASSWORD="secure-random-password"
   export ADMIN_NAME="Администратор"
   ```

2. Запустите развертывание:

   ```bash
   npm run db:migrate
   npm run db:seed
   ```

3. **Обязательно** смените пароль после первого входа

## Мониторинг

### Логи создания администратора

```bash
# Development
🔑 Учетные данные администратора:
   Email: admin@parket-crm.ru
   Пароль: a1b2c3d4e5f6...
   ⚠️  ОБЯЗАТЕЛЬНО смените пароль после первого входа!

# Production
✅ Создан пользователь-администратор: admin@parket-crm.ru
✅ Использован пароль из переменной окружения
```

### Проверка статуса

```bash
# Проверка существования администратора
npm run db:check-admin

# Статистика пользователей
npm run db:stats
```

## Troubleshooting

### Проблема: Администратор не создается

**Решение:**

1. Проверьте подключение к базе данных
2. Убедитесь, что миграции применены
3. Проверьте логи seed процесса

### Проблема: Не могу войти как администратор

**Решение:**

1. Проверьте правильность email и пароля
2. Убедитесь, что пароль не был изменен
3. Проверьте логи аутентификации

### Проблема: Ошибка при смене пароля

**Решение:**

1. Проверьте правильность текущего пароля
2. Убедитесь, что новый пароль соответствует требованиям
3. Проверьте права доступа к API

## Лучшие практики

### Безопасность

1. **Всегда** используйте сложные пароли
2. **Обязательно** смените пароль после первого входа
3. **Регулярно** обновляйте пароли администраторов
4. **Мониторьте** логи входа администраторов

### Управление

1. Используйте переменные окружения для настройки
2. Ведите журнал изменений паролей
3. Регулярно проверяйте права доступа
4. Создавайте резервные копии базы данных

### Мониторинг

1. Настройте уведомления о входе администраторов
2. Мониторьте подозрительную активность
3. Ведите логи всех административных действий
4. Регулярно проверяйте целостность системы
