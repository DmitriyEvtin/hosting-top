# ===========================================
# Паркет Retail - ПЕРЕМЕННЫЕ ОКРУЖЕНИЯ ДЛЯ MINIO
# ===========================================

# ===========================================
# ОСНОВНЫЕ НАСТРОЙКИ ПРИЛОЖЕНИЯ
# ===========================================
NODE_ENV="development"
APP_NAME="Паркет Retail"
APP_VERSION="1.0.0"

# ===========================================
# БАЗА ДАННЫХ
# ===========================================
# PostgreSQL подключение
DATABASE_URL="postgresql://parket_retail_user:parket_retail_password@localhost:5432/parket_retail?schema=public"

# ===========================================
# REDIS (КЭШИРОВАНИЕ И СЕССИИ)
# ===========================================
REDIS_URL="redis://localhost:6379"

# ===========================================
# АУТЕНТИФИКАЦИЯ (NextAuth.js)
# ===========================================
NEXTAUTH_SECRET="your-secret-key-here-change-in-production"
NEXTAUTH_URL="http://localhost:3000"

# OAuth провайдеры
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""
GITHUB_CLIENT_ID=""
GITHUB_CLIENT_SECRET=""

# Российские OAuth провайдеры
VK_CLIENT_ID=""
VK_CLIENT_SECRET=""
OK_CLIENT_ID=""
OK_CLIENT_SECRET=""
MAIL_CLIENT_ID=""
MAIL_CLIENT_SECRET=""
YANDEX_CLIENT_ID=""
YANDEX_CLIENT_SECRET=""

# ===========================================
# АДМИНИСТРАТОР ПО УМОЛЧАНИЮ
# ===========================================
# Настройки для создания первого администратора
# Если не указаны, будут использованы значения по умолчанию
ADMIN_EMAIL="admin@parket-crm.ru"
ADMIN_PASSWORD=""
ADMIN_NAME="Администратор"

# ===========================================
# MINIO S3 (ЛОКАЛЬНОЕ ХРАНЕНИЕ ИЗОБРАЖЕНИЙ)
# ===========================================
# MinIO credentials (для локальной разработки)
AWS_ACCESS_KEY_ID="minioadmin"
AWS_SECRET_ACCESS_KEY="minioadmin123"
AWS_REGION="us-east-1"
AWS_S3_BUCKET="parket-retail-images"

# MinIO endpoint (локальный)
AWS_S3_ENDPOINT="http://localhost:9000"
AWS_S3_FORCE_PATH_STYLE="true"

# CloudFront CDN (отключен для локальной разработки)
CLOUDFRONT_DOMAIN=""

# ===========================================
# НАСТРОЙКИ ПАРСИНГА
# ===========================================
PARSING_BATCH_SIZE="10"
PARSING_DELAY_MS="1000"
PARSING_MAX_RETRIES="3"
PARSING_USER_AGENT="Mozilla/5.0"

# ===========================================
# МОНИТОРИНГ И ЛОГИРОВАНИЕ
# ===========================================
# Sentry DSN для серверной части
SENTRY_DSN=""
# Sentry DSN для клиентской части (публичный)
NEXT_PUBLIC_SENTRY_DSN=""
# Sentry организация и проект
SENTRY_ORG=""
SENTRY_PROJECT=""
LOG_LEVEL="debug"

# ===========================================
# EMAIL (УВЕДОМЛЕНИЯ)
# ===========================================
# Для development с MailHog:
SMTP_HOST="localhost"
SMTP_PORT="1025"
SMTP_USER=""
SMTP_PASSWORD=""
SMTP_FROM="noreply@parket-crm.local"

# ===========================================
# БЕЗОПАСНОСТЬ
# ===========================================
CORS_ORIGIN="http://localhost:3000"
RATE_LIMIT_MAX="100"
RATE_LIMIT_WINDOW_MS="900000"

# ===========================================
# РАЗРАБОТКА (только для development)
# ===========================================
# Раскомментируйте для отладки
# DEBUG="*"
# VERBOSE_LOGGING="true"
